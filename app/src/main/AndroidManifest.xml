<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Calculator"
        tools:targetApi="31">

        <!-- Set WelcomeActivity as the launcher activity -->
        <activity
            android:name="com.innovativebits.multicalculator.WelcomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Other activities -->
        <activity
            android:name="com.innovativebits.multicalculator.MainActivity"
            android:exported="true" />
        <activity
            android:name="com.innovativebits.multicalculator.SimpleCalculatorActivity"
            android:exported="true" />
        <activity
            android:name="com.innovativebits.multicalculator.UnitConverterActivity"
            android:exported="true" />
        <activity
            android:name="com.innovativebits.multicalculator.BinaryConverterActivity"
            android:exported="true" />
    </application>

</manifest>
